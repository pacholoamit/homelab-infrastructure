apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "developer-portfolio.fullname" . }}-test-connection"
  labels:
    {{- include "developer-portfolio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "developer-portfolio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
