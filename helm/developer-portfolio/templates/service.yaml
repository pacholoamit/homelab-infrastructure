apiVersion: v1
kind: Service
metadata:
  name: {{ include "developer-portfolio.fullname" . }}
  labels:
    {{- include "developer-portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "developer-portfolio.selectorLabels" . | nindent 4 }}
