apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kyverno
  namespace: kyverno
spec:
  interval: 30m
  chart:
    spec:
      chart: kyverno
      # Bumped to 3.7.2 (Kyverno v1.17.2) after k3s 1.29 -> 1.34 upgrade
      # refreshed every node's containerd image cache (resolves the prior
      # post-NIC-incident image-blob corruption that forced a pin to 3.7.1).
      version: "3.7.2"
      sourceRef:
        kind: HelmRepository
        name: kyverno
        namespace: flux-system
      interval: 1h
  values:
    admissionController:
      replicas: 3
      container:
        image:
          pullPolicy: Always
      initContainer:
        image:
          pullPolicy: Always
      podAntiAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - admission-controller
            topologyKey: kubernetes.io/hostname
      tolerations:
        - key: node.kubernetes.io/unreachable
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 30
        - key: node.kubernetes.io/not-ready
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 30
    backgroundController:
      replicas: 1
    cleanupController:
      replicas: 1
    reportsController:
      replicas: 1
    features:
      admissionReports:
        enabled: true
      backgroundScan:
        enabled: false
