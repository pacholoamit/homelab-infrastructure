apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: eraser
  namespace: flux-system
spec:
  interval: 5m
  install:
      createNamespace: true
  chart:
    spec:
      chart: eraser
      version: "1.3.x"
      sourceRef:
        kind: HelmRepository
        name: eraser
        namespace: flux-system
      interval: 1m
