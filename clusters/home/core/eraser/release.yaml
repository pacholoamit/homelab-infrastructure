apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: eraser
  namespace: eraser-system
spec:
  interval: 5m
  install:
      createNamespace: true
  chart:
    spec:
      chart: eraser
      version: "1.3.x"
      sourceRef:
        kind: HelmRepository
        name: eraser
        namespace: eraser-system
      interval: 1m
