apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperclip-claude-bin
  namespace: paperclip
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
