apiVersion: v1
kind: Namespace
metadata:
  name: github-runners
  labels:
    # The runners run whatever a ZenOS workflow says. Baseline admission keeps
    # them unprivileged, off host namespaces and off hostPath volumes; sudo
    # inside the container still works, which Playwright's --with-deps needs.
    pod-security.kubernetes.io/enforce: baseline
